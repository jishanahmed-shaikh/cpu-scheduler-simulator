# The CPU Scheduler Game runs fully offline and needs no environment variables.
# This file exists only to document that fact and reserve a place for future
# build-time toggles. Copy to `.env` if you ever need local overrides.

# Vite dev server port (default 5173)
# VITE_PORT=5173
